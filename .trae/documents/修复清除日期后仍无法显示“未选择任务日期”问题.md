## 结论与原因
- 清除逻辑本身正确：`editTodoDueDate` 的清除分支将 `dueDate` 设为 `null` 并重渲染，代码在 /Users/dl/Documents/todo-web/main.js:619–629。
- 问题出在渲染文案：若仍使用旧写法 `任务日期：${todo.dueDate || "未选择任务日期"}`，清除后会显示“任务日期：未选择任务日期”。应改为无日期时只显示“未选择任务日期”。
- 当前文件已存在条件渲染（/Users/dl/Documents/todo-web/main.js:382–385），但若仍复现，可能是：
  1) 本地仍有旧代码片段未更新；
  2) 有其他路径在重渲染后覆盖了文案；
  3) `dueDate` 被错误写回为空字符串而非 `null`（虽然现有条件也能处理）。

## 修改点
1. 强化任务项日期渲染（再次确保条件逻辑一致）：
   - 在 `renderTodos()` 中，使用：
     - 有日期：`任务日期：YYYY-MM-DD`
     - 无日期：`未选择任务日期`
   - 位置：/Users/dl/Documents/todo-web/main.js:382–385
2. 清除逻辑稳固：
   - 保持 `handleClear` 将 `dueDate` 设为 `null`，并在执行前移除全局 `mousedown` 监听避免竞态。
   - 位置：/Users/dl/Documents/todo-web/main.js:619–629
3. 防止外部点击/blur 覆盖清除：
   - 已通过 `finished` 标志阻止后续分支触发；保留该保护。
   - 位置：`editTodoDueDate` 内的 `onDocMouseDown` 与 `blur` 处理。
4. 明确布尔判断：
   - 使用显式判断 `todo.dueDate !== null && todo.dueDate !== ""`，避免非期望类型影响显示。

## 验证用例
- 初始有日期 → 点击“清除” → 显示“未选择任务日期”。
- 初始无日期 → 点击“清除” → 仍显示“未选择任务日期”。
- 选择新日期 → 点击页面空白 → 显示“任务日期：YYYY-MM-DD”。
- 快速点击“清除”后立即点击外部 → 不会被外部点击覆盖；显示仍为“未选择任务日期”。

## 执行计划
- 更新并统一 `renderTodos()` 的日期文案逻辑。
- 复核 `editTodoDueDate` 的清除分支与事件清理。
- 进行端到端验证，确保所有用例通过。请确认后我将直接修改并验证。