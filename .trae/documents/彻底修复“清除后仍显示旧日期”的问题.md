## 问题复核
- 清除分支：/Users/dl/Documents/todo-web/main.js:619–629 将 `dueDate=null` 并重渲染。
- 渲染文案：/Users/dl/Documents/todo-web/main.js:382–386 无日期只显示“未选择任务日期”。
- 仍复现的可能原因：
  - 事件竞态：文档级 `mousedown` 捕获在清除按钮点击前先触发，外部提交覆盖清除。
  - 输入框仍保留旧值，竞态路径使用它写回旧日期。

## 修复思路
- 阻止竞态：在清除按钮上阻止事件传播，避免全局 `mousedown` 捕获干扰。
- 明确清空输入：清除时将 `input.value=""`，确保即便发生失焦/外部点击也不会写回旧日期。
- 加强保护：清除分支先 `finished=true`，再移除全局监听；在 `blur`/外部点击中优先检查 `finished`。

## 具体改动
- 在 `editTodoDueDate(...)`：
  - `clearBtn.addEventListener("mousedown", e => e.stopPropagation())`
  - `clearBtn.addEventListener("click", handleClear)` 中，先将 `finished=true`，`document.removeEventListener("mousedown", ...)`，`input.value=""`，`todos` 写入 `null`，`saveToStorage()`，`renderTodos()`。
- 保持外部点击与 `blur` 的提交逻辑仅在未 `finished` 且 `input.value` 非空时执行。

## 验证用例
- 初始有日期→点击“清除”→显示“未选择任务日期”。
- 初始有日期→点击“清除”后立刻点击页面空白→仍显示“未选择任务日期”。
- 选择新日期→点击页面空白→显示“任务日期：YYYY-MM-DD”。

请确认后我将按上述方案修改并进行端到端验证。